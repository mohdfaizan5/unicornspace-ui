---
isPublished: false
title: Production Checklist for Nextjs14 developers.
description: WIP
---

1. package.json file

```json
{
  // ...
  "scripts": {
    // ...
    "typecheck": "tsc --noEmit",
    "postinstall": "prisma generate"
  },
  // for seed file
  "prisma": {
    "seed": "ts-node --compiler-options {\"module\":\"CommonJS\"} prisma/seed.ts"
  }
}
```

2. Env variables.

```

```

3. Prisma (first time)

- Step 1: Make you're data base as same as your schema.prisma file.
  `prisma db push`
- Step 2: Seed the file if you have seed file.
  `prisma seed`
- add to package.json file

```json
{
  "scripts": {
    "postinstall": "prisma generate"
  }
}
```
