---
isPublished: false
title: React Email Resend
description: This guide will help you to resend the email in React.
tags: react, email, resend
thumbnail: thumbnail.png (thumbnail should be in the public/images/guide folder)
author: Faizan
---

# Installation

Follow this link for installation of react email [official](https://react.email/docs/getting-started/manual-setup)

<Steps>
  <Step>
  Install the following dependencies:
  ```bash
  pnpm add react-email -D -E
  pnpm add @react-email/components react react-dom -E
  ```
  </Step>
<Step>
  ```tsx

````
</Step>
<Step>
```tsx

````

  </Step>
  <Step>
  react email expects your emails to in src but since we'll use it in custom place lets add
  ```json:pacakge.json
{
  "scripts": {
    "email dev": "email dev --dir src/components/emails",
  }
}
  ```
  </Step>
  <Step>
  Create a new folder called `emails/components/` in components and add a new file called `welcome.tsx` with the following content:
  
  ```tsx:welcome.tsx
import { Button, Html } from "@react-email/components";
import * as React from "react";

export default function Email() {
  return (
    <Html>
      <Button
        href="https://example.com"
        style={{ background: "#000", color: "#fff", padding: "12px 20px" }}
      >
        Click me
      </Button>
    </Html>
  );
}
  ```
  </Step>
  <Step>
  run locally
  ```bash
  pnpm run email
  ```
  </Step>

</Steps>

## Installing resend

A React component to resend emails.

## Installing Tailwind

A React component to wrap emails with Tailwind CSS.
{/* (official docs)[https://react.email/docs/components/tailwind] */}

```bash
pnpm add @react-email/components -E

# or get the individual package

pnpm add @react-email/tailwind -E
```

Wrap the component with the Tailwind component.

```tsx
import { Tailwind, Button } from "@react-email/components";

const Email = () => {
  return (
    <Tailwind
      config={{
        theme: {
          extend: {
            colors: {
              brand: "#007291",
            },
          },
        },
      }}
    >
      <Button
        href="https://example.com"
        className="bg-brand px-3 py-2 font-medium leading-4 text-white"
      >
        Click me
      </Button>
    </Tailwind>
  );
};

export default Email;
```
